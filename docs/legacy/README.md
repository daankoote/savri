# ENVAL Legacy Documentation

Status: LEGACY / SUPERSEDED.

This directory contains documentation from earlier ENVAL product generations.

Legacy files are not source of truth for:

- `/app`
- `api-app-*`
- `app_*` tables
- current ENVAL commercial inboekservice positioning

Current canon starts at:

- `docs/app/00_CANON.md`

Legacy documents may only be reused after deliberate adaptation into `docs/app/`.

## Directories

- `root-static/`: old root/static production and legacy dossier generation docs.
- `tooling/`: old or unsafe tooling notes. Treat as reference only.
