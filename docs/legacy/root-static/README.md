<!--
LEGACY / SUPERSEDED
This file belongs to the old root/static ENVAL product generation.
It is not source of truth for /app, api-app-*, or app_* implementation.
Reuse is allowed only after explicit adaptation into docs/app/.
Current canon starts at docs/app/00_CANON.md.
-->

# Root/Static Legacy Documentation

Status: LEGACY / SUPERSEDED.

This directory contains the old root/static ENVAL documentation set. It belongs to the previous static site, legacy `api-dossier-*` functions, legacy `dossier_*` tables, and older business assumptions.

It is not source of truth for:

- `/app`
- `api-app-*`
- `app_*` tables
- the current customer-facing commercial ENVAL inboekservice

Current canon starts at:

- `docs/app/00_CANON.md`

Important:

- `03_CHANGELOG_APPEND_ONLY.md` is preserved as historical append-only record. Its body was not rewritten during the canon migration.
- Other files may contain old statements such as neutral infrastructure, not-inboeker, private-only, max-4, or fixed export-fee models. Those are historical only.
- Reuse requires explicit adaptation into `docs/app/`.
